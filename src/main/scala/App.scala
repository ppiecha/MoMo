object App extends App{
  if (args.isEmpty) throw new IllegalArgumentException("Undefined composition path")
  else ???
    //open file
    //map to yaml composition case class
    //load
    //play
}
